## ── Node / magpie experiments ──────────────────────────────────────────────
node_modules/
dist/

## ── Environment / secrets ──────────────────────────────────────────────────
.env
.env.local

## ── Claude / AI tooling ────────────────────────────────────────────────────
CLAUDE.md
.claude/

## ── macOS ───────────────────────────────────────────────────────────────────
.DS_Store

## ── LaTeX auxiliary / build files ──────────────────────────────────────────
# latexmk routes aux files to writing/ConsensusMarker/build/ via .latexmkrc
writing/ConsensusMarker/build/
# Fallback globs catch any aux files that land outside build/
*.aux
*.bbl
*.bcf
*.blg
*.fdb_latexmk
*.fls
*.log
*.lof
*.lot
*.out
*.run.xml
*.synctex.gz
*.synctex\(busy\)
*.toc
*.idx
*.ilg
*.ind
*.glo
*.gls
*.glg
*.acn
*.acr
*.alg
*.ist
*.nav
*.snm
*.vrb
*.xdv
*.dvi
# Keep compiled PDFs tracked; uncomment to ignore them too:
# *.pdf
.vscode/settings.json
